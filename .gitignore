# Python / env
.venv/
venv/
env/
__pycache__/
*.pyc

# Data (too large for git)
data/
data/raw/
data/processed/
checkpoints/
models/

# Note: outputs/ IS committed (small, only 2-3 MB)

# Hugging Face cache
.cache/
.cache/huggingface/
.hf/
.huggingface/

# IDE/editor
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# OS
.DS_Store
Thumbs.db

# Secrets
.env
*.pem
*.key

# Local tokenized dataset zip (do not commit)
fever_tokenized_distilbert.zip

# Data and processed artifacts (do not commit)
data/
data/raw/
data/processed/
checkpoints/
models/

# Allow committing `src/models` (we ignore top-level/other models folders but keep src models)
!src/models/
!src/models/**

# Local virtual envs
.venv/
venv/
env/
